<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

  <strong>Profile Image:</strong>
<%= image_tag @user.profile_image.variant(resize_to_limit: [500, 500]) if @user.profile_image.attached? %>
<%= link_to @user.profile_image.filename, @user.profile_image if @user.profile_image.attached? %>
